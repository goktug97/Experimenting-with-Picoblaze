loop:
    input sA, 40
    test sA, FF
    jump nz, main 
    test sA, FF
    jump nz, main 
    jump loop

main:
    input s0, 04
    load sF, sF
    load sF, sF
    input s1, 80
    load sF, sF
    load sF, sF
    xor s0, s1
    output s0, C7
    jump loop
